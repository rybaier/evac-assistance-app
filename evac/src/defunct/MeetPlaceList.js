import React from 'react'
import {Text, View, StyleSheet, FlatList } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const MeetPlaceList = () => {
    return (
       <FlatList />
    )
}

const styles = StyleSheet.create({

})

export default MeetPlaceList